# Public demo safety — local only

Current workaround: visitors use the live team's open search/submission flow.
Job: explore a fictional case without reading or writing team records.

1. Separate the public demo and verify at 390px and 1440px.
   Acceptance: three fictional examples; no private data in anonymous API responses or frontend assets; anonymous mutations denied; staff views locked; search/no-match/escalation preview work; 44px targets; no input overlay or horizontal overflow.

No deployment, database cleanup, seed updates, PIN changes, or new team login system. Existing staff sign-in retained. Test artifacts filtered, not deleted. Logged-out teammates now see the demo and cannot submit real cases.

Live protection remains unchanged until separately approved deployment. Review/rotate existing shared PINs before publishing; never store PINs here.
